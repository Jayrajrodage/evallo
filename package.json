{
  "name": "evallo-assessment",
  "private": true,
  "scripts": {
    "install": "concurrently \"cd backend && npm install\" \"cd frontend && npm install\" \"npm install\"",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
